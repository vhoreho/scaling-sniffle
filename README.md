# CRUD приложение с авторизацией

Полнофункциональное приложение с CRUD операциями, авторизацией и защитой страниц.

## Стэк технологий

- **Frontend**: React + TypeScript + Shadcn UI + Vite
- **Backend**: Express + TypeScript + Prisma
- **База данных**: SQLite (разработка) / PostgreSQL (продакшен)

## Функциональность

- ✅ Регистрация и вход пользователей
- ✅ Защищенные и открытые страницы
- ✅ Полный CRUD для постов
- ✅ Сохранение токенов в cookies
- ✅ Современный UI с Shadcn компонентами

## Установка

### 1. Установка зависимостей

```bash
# Установка зависимостей для всего проекта
npm install

# Или отдельно для backend и frontend
cd backend && npm install
cd ../frontend && npm install
```

### 2. Настройка базы данных

```bash
cd backend

# Создание .env файла из примера
cp .env.example .env
# Или на Windows:
# copy .env.example .env

# Отредактируйте .env и измените JWT_SECRET на свой секретный ключ

# Генерация Prisma клиента
npm run prisma:generate

# Создание миграций
npm run prisma:migrate
```

### 3. Запуск приложения

В корне проекта:

```bash
# Запуск backend и frontend одновременно
npm run dev

# Или отдельно:
npm run dev:backend  # Backend на http://localhost:3001
npm run dev:frontend # Frontend на http://localhost:5173
```

## Структура проекта

```
├── backend/
│   ├── src/
│   │   ├── routes/       # API маршруты
│   │   ├── middleware/   # Middleware для авторизации
│   │   ├── lib/          # Утилиты (Prisma, Auth)
│   │   └── server.ts     # Точка входа сервера
│   ├── prisma/
│   │   └── schema.prisma # Схема базы данных
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── pages/        # Страницы приложения
│   │   ├── context/      # React Context (Auth)
│   │   ├── lib/          # API клиент и утилиты
│   │   └── App.tsx       # Главный компонент
│   └── package.json
└── package.json
```

## API Endpoints

### Авторизация

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Получить текущего пользователя

### Посты (требуют авторизации)

- `GET /api/posts` - Получить все посты пользователя
- `GET /api/posts/:id` - Получить один пост
- `POST /api/posts` - Создать пост
- `PUT /api/posts/:id` - Обновить пост
- `DELETE /api/posts/:id` - Удалить пост

## Использование

1. Откройте приложение в браузере: `http://localhost:5173`
2. Зарегистрируйтесь или войдите в систему
3. На странице Dashboard вы можете создавать, редактировать и удалять посты

## Деплой на Render

Подробная инструкция по деплою приложения на Render доступна в файле [DEPLOY.md](./DEPLOY.md).

### Быстрый старт:

1. Подключите репозиторий к Render
2. Используйте файл `render.yaml` для автоматического создания всех сервисов (Blueprint)
3. Настройте переменные окружения согласно инструкции в DEPLOY.md

Приложение готово к деплою на Render! 🚀
